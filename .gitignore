# ── Secrets / local config ───────────────────────────────
.env
mcp-*.json
.openclaw

# ── Node ──────────────────────────────────────────────────
node_modules/
package-lock.json

# ── .NET build output (regenerated by `dotnet build`) ─────
autocad-plugin/bin/
autocad-plugin/obj/
*.user

# ── AutoCAD / project data ────────────────────────────────
*.dwg
*.bak
outputs/
# Auto-generated temp scripts (keep directory, ignore generated files)
temp/*.lsp
temp/*.scr

# ── OS / shell artifacts ──────────────────────────────────
*.lnk
Thumbs.db
desktop.ini

# ── CI ────────────────────────────────────────────────────
.github/
